[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 5
startCommand = "/usr/local/bin/adp-server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
